{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": [
        "**/.env.*local"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                ".next/**",
                "!.next/cache/**"
            ],
            "env": [
                "DATABASE_URL",
                "OPENROUTER_API_KEY",
                "OPENROUTER_MODEL",
                "NEXT_PUBLIC_PRIVY_APP_ID",
                "PRIVY_APP_SECRET"
            ]
        },
        "lint": {
            "dependsOn": [
                "^lint"
            ]
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
